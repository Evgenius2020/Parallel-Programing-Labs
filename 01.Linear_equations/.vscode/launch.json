{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "cwd": ".",
            "program": "${workspaceFolder}/out/a.out",
            "preLaunchTask": "debug build"
        }
    ]
}