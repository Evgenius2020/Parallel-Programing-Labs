{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "mpicc src/*.c -o out/a.out",
            "group": "build"
        }
    ]
}