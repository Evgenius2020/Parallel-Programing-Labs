{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "mpicc src/*.c -o out/a.out -lm",
            "group": "build"
        },
        {
            "label": "debug build",
            "type": "shell",
            "command": "mpicc -O0 -ggdb3 src/*.c -o out/a.out",
            "group": "build"
        }
    ]
}