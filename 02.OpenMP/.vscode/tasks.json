{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc src/*.c -lm -fopenmp -o out/a.out",
            "group": "build"
        }
    ]
}